# Client-only dependencies — installed on the user's local machine.
# The server (Cloud Run) uses requirements-cloud.txt instead.

# WebSocket client
websockets>=13.0

# Audio IO
sounddevice>=0.4.6
numpy>=1.26.0
certifi>=2024.12.0

# Browser automation
playwright>=1.50.0
pynput==1.8.1
pyobjc-framework-Cocoa==12.1
pyobjc-framework-Quartz==12.1

# Hand tracking cursor
opencv-python>=4.10.0
mediapipe>=0.10.14

# Screenshot / image processing
Pillow>=10.0
